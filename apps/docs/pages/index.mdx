---
layout: landing
showSidebar: false
---

import { HomePage } from 'vocs'

<HomePage.Root>
  <HomePage.Logo />
  <HomePage.Tagline>The TypeScript toolkit for Cartesi</HomePage.Tagline>
  <HomePage.Description>A collection of TypeScript libraries to build, run, and observe Cartesi applications.</HomePage.Description>
</HomePage.Root>

<div className="home-cards">
  <a className="home-card" href="/app/quick-start">
    <div className="home-card-header">
      <h3>App</h3>
      <span className="home-card-packages"><code>@deroll/app</code>, <code>@deroll/wallet</code></span>
    </div>
    <p>Build Cartesi application backends in TypeScript — handle inputs, manage assets, and emit outputs.</p>
  </a>
  <a className="home-card" href="/cmio/getting-started">
    <div className="home-card-header">
      <h3>Bindings</h3>
      <span className="home-card-packages"><code>@deroll/cm</code>, <code>@deroll/cmio</code></span>
    </div>
    <p>Low-level Node.js bindings for the guest rollup library and the Cartesi Machine itself.</p>
  </a>
  <a className="home-card" href="/explorer">
    <div className="home-card-header">
      <h3>Explorer</h3>
      <span className="home-card-packages"><code>@deroll/decoder</code>, explorer.deroll.dev</span>
    </div>
    <p>A web UI to navigate a rollups node, plus a toolkit for decoding payloads.</p>
  </a>
</div>

<style>{`
  .home-cards {
    display: grid;
    grid-template-columns: 1fr;
    gap: 16px;
    max-width: 1120px;
    margin: 48px auto 0;
    padding: 0 24px;
  }
  @media (min-width: 768px) {
    .home-cards {
      grid-template-columns: repeat(3, 1fr);
    }
  }
  .home-card {
    display: block;
    padding: 24px;
    border: 1px solid var(--vocs-color_border, rgba(125,125,125,0.25));
    border-radius: 12px;
    background-color: var(--vocs-color_background2, rgba(125,125,125,0.04));
    text-decoration: none;
    transition: border-color 0.15s ease, background-color 0.15s ease, transform 0.15s ease;
  }
  .home-card:hover {
    border-color: var(--vocs-color_textAccent, #ff6b35);
    background-color: var(--vocs-color_background3, rgba(125,125,125,0.08));
    transform: translateY(-2px);
  }
  .home-card-header {
    display: flex;
    align-items: baseline;
    justify-content: space-between;
    gap: 12px;
    flex-wrap: wrap;
    margin: 0 0 8px;
  }
  .home-card h3 {
    margin: 0;
    font-size: 1.2rem;
    color: var(--vocs-color_heading, inherit);
  }
  .home-card-packages {
    font-size: 0.8rem;
    color: var(--vocs-color_text3, var(--vocs-color_text2, #888));
    opacity: 0.7;
    text-align: right;
  }
  .home-card-packages code {
    font-size: 0.8rem;
    color: inherit;
    background: none;
    padding: 0;
  }
  .home-card p {
    margin: 0;
    font-size: 0.95rem;
    line-height: 1.55;
    color: var(--vocs-color_text3, var(--vocs-color_text2, #888));
  }
`}</style>
