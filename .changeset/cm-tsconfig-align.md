---
"@deroll/cm": patch
---

Align the TypeScript configuration with the monorepo: extend `@deroll/tsconfig/base.json` (NodeNext, ES2022, strict), add `.js` extensions to relative imports, and upgrade to TypeScript 6. Internal tooling change — the public API is unchanged.
