name: CI
on:
    pull_request:
    push:
        branches:
            - main
            - release/*
            - prerelease/*
    workflow_dispatch:
permissions:
    contents: read
concurrency:
    group: ci-${{ github.workflow }}-${{ github.ref }}
    cancel-in-progress: true

jobs:
    build:
        name: Lint, build, test
        runs-on: ubuntu-latest
        steps:
            - name: Checkout Repo
              uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
              with:
                  # genext2fs's native addon compiles from the genext2fs and
                  # libarchive submodules (deps/) during `bun install`, so they
                  # must be checked out.
                  submodules: recursive

            - name: Setup Bun
              uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0

            - name: Setup Node.js
              uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
              with:
                  node-version: 24

            # ubuntu-latest already ships gcc/g++/make/python3, which node-gyp
            # needs to compile the genext2fs addon on install.
            - name: Install Dependencies
              run: bun install --frozen-lockfile

            - name: Lint
              run: bun run lint

            - name: Build
              run: bun run build

            - name: Test
              run: bun run test
