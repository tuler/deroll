---
"@deroll/decoder": minor
---

Add `@deroll/decoder` — the typed toolkit for writing Cartesi Node Explorer payload decoders (migrated from `@tuler/luke-decoder`). Part of bringing the explorer into the monorepo: the explorer site (`@deroll/explorer`) and its private companions (`@deroll/json-decoder`, `@deroll/mock-server`) come along too.
