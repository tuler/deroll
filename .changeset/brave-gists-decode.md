---
"@deroll/explorer": minor
---

load payload decoders from GitHub gists — paste a gist page URL (the decoder file is found automatically, or picked via its `#file-…` anchor), a per-file Raw URL, or the `gist:id/file.ts` shorthand
